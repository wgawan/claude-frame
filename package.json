{
  "name": "claude-frame",
  "version": "1.0.0",
  "description": "Simple web UI to invoke Claude Code via SSE",
  "license": "MIT",
  "type": "module",
  "bin": {
    "claude-frame": "./server.js"
  },
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js"
  },
  "engines": {
    "node": ">=20"
  }
}
